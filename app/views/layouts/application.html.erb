<!DOCTYPE html>
<html>
<head>
  <title>Edunova</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>

</head>
<% if current_user %>
<body>
  <% else %>
<body class="<%= controller.controller_name %>">
<% end %>



<header>
 <div class="topper"></div>




 <div class="container">
  <!-- Static navbar -->
  <div class="navbar navbar-static-top" role="navigation">
   <div class="navbar-header">
    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
     <span class="sr-only">Toggle navigation</span>
     <span class="icon-bar"></span>
     <span class="icon-bar"></span>
     <span class="icon-bar"></span>
    </button>
        <a href="/" class="navbar-brand logo-grp"><span class="logo-block"></span></a>
        <a href="/" class="brand-name"> edunova.io </a>

   </div>
   <div class="navbar-collapse collapse">
    <ul class="nav navbar-nav navbar-right">

    <li id="home_nav" class="<%= 'active'  if current_page?(root_path) %>">
<div id="<%= 'arrow' if current_page?(root_path) %>"> </div> <%= link_to "Home" , root_path %> </li>

    <li id="learn_nav" class="<%= 'active'  if current_page?(notes_path) %>">
 <div id="<%= 'arrow' if current_page?(notes_path) %>"> </div> <%= link_to "Learn" , notes_path %></li>

<% if current_user %>
  <li id="social_nav" class="<%= 'active'  if current_page?(feeds_path) %>">
   <div id="<%= 'arrow' if current_page?(feeds_path) %>"> </div><%= link_to "Connect" , feeds_path %></li>

  <li id="profile_nav" class="<%= 'active'  if current_page?(profile_path(current_user)) %>">
    <div id="<%= 'arrow' if current_page?(profile_path(current_user)) %>"> </div>
    <%= link_to "Profile" , profile_path(current_user) %></li>

  <li><%= link_to "Logout" , destroy_user_session_path , :method => "get" %></li>
<% else %>
 


  <li id="social_nav" class="<%= 'active'  if current_page?(new_user_registration_path) %>">    <div id="<%= 'arrow' if current_page?(new_user_registration_path) %>"> </div>

    <%= link_to "Sign Up" ,  new_user_registration_path %></li>


  <li id="social_nav" class="<%= 'active'  if current_page?(home_about_us_path) %>"><%= link_to "Contact" , home_about_us_path %>
        <div id="<%= 'arrow' if current_page?(home_about_us_path) %>"> </div>

  </li>


<% end %>



    </ul>
   </div>
  </div>
 </div>


</header>











   

            <div class="container">
  
    



<%= yield %>
<div class="well">

<% if user_signed_in? %>
  <% if user_signed_in? && !current_user.confirmed? %>
  <div>
    Please confirm your account by clicking the link in the email we sent to <%= current_user.email %>
  </div>
<% end %>
  Logged in as <strong><%= current_user.email %></strong>.

       
<%= link_to 'Change Password', edit_user_registration_path %>
 

<% end %>
<% if notice %>
<span class="notice"><%= notice %></span> <% end %>
<% if alert %>
       <span class="alert"><%= alert %></span> <% end %>
</div>

       
</div>

</body>
</html>
